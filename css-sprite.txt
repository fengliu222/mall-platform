/*
以下是css sprite生成插件自动生成的LESS变量，储存了一些全站通用的图片。

usage:
  .sprite-width(@sprite)
  .sprite-height(@sprite)
  .sprite-position(@sprite)
  .sprite-image(@sprite)
  .sprite(@sprite)

@sprite就是下面对应文件名字的变量，比如@logo.
一般使用的时候直接使用.sprite(@sprite)方法就可以快捷的创建好元素了
By moe.
*/